---
phase: 01-foundation-data
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/setup.ts
  - tests/pwa.test.ts
  - tests/db/crud.test.ts
  - tests/persistence.test.ts
  - tests/offline.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Test framework is configured and runnable"
    - "All test file scaffolds exist with placeholder tests"
    - "Running npm test executes without configuration errors"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
    - path: "tests/setup.ts"
      provides: "Test setup with fake-indexeddb"
      contains: "fake-indexeddb"
    - path: "tests/pwa.test.ts"
      provides: "PWA smoke test scaffold"
      min_lines: 10
    - path: "tests/db/crud.test.ts"
      provides: "CRUD test scaffold"
      min_lines: 10
    - path: "tests/persistence.test.ts"
      provides: "Persistence test scaffold"
      min_lines: 10
    - path: "tests/offline.test.ts"
      provides: "Offline test scaffold"
      min_lines: 10
  key_links: []
---

<objective>
Create the complete test infrastructure for Phase 1 before any implementation begins.

Purpose: Per Nyquist compliance, test files must exist before they can be referenced in verify commands. This Wave 0 plan creates all test scaffolds with placeholder tests that will be populated during implementation.

Output: Configured Vitest with all test file scaffolds ready for implementation to fill in.
</objective>

<execution_context>
@C:/Users/jshepherd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshepherd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data/01-CONTEXT.md
@.planning/phases/01-foundation-data/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test dependencies and create Vitest configuration</name>
  <files>package.json, vitest.config.ts, tests/setup.ts</files>
  <action>
Note: This task runs BEFORE the main Vite project is scaffolded. Initialize npm first if package.json doesn't exist.

1. If package.json doesn't exist, run: `npm init -y`
2. Install test dependencies:
```bash
npm install -D vitest fake-indexeddb @vitest/coverage-v8 jsdom
```

3. Create vitest.config.ts:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
  },
});
```

4. Create tests/setup.ts:
```typescript
import 'fake-indexeddb/auto';
```

5. Add test script to package.json: `"test": "vitest"`
  </action>
  <verify>
    <automated>npm test -- --run --passWithNoTests</automated>
  </verify>
  <done>Vitest runs without configuration errors. Test command exits cleanly with passWithNoTests flag.</done>
</task>

<task type="auto">
  <name>Task 2: Create all test file scaffolds with placeholder tests</name>
  <files>tests/pwa.test.ts, tests/db/crud.test.ts, tests/persistence.test.ts, tests/offline.test.ts</files>
  <action>
Create test file scaffolds with skip markers. These will be filled in during implementation.

Create tests/pwa.test.ts:
```typescript
import { describe, it, expect } from 'vitest';

describe('PWA Infrastructure', () => {
  it.todo('manifest includes correct app name');
  it.todo('manifest includes 192x192 icon');
  it.todo('manifest includes 512x512 icon');
  it.todo('service worker registers successfully');
});
```

Create tests/db/ directory and tests/db/crud.test.ts:
```typescript
import 'fake-indexeddb/auto';
import { describe, it, expect, beforeEach } from 'vitest';

describe('Person CRUD', () => {
  describe('add person', () => {
    it.todo('creates record with auto-generated id');
    it.todo('stores optional notes field');
  });

  describe('update person', () => {
    it.todo('merges with existing data');
    it.todo('updates the updatedAt timestamp');
  });

  describe('delete person', () => {
    it.todo('removes record by id');
    it.todo('does not throw on non-existent id');
  });
});
```

Create tests/persistence.test.ts:
```typescript
import 'fake-indexeddb/auto';
import { describe, it, expect, beforeEach } from 'vitest';

describe('Data Persistence', () => {
  it.todo('persists data across database reconnection');
  it.todo('persists multiple records');
  it.todo('updates persist correctly');
  it.todo('deletes persist correctly');
});
```

Create tests/offline.test.ts:
```typescript
import { describe, it, expect } from 'vitest';

describe('Offline Functionality', () => {
  it.todo('static assets are cached by service worker');
  it.todo('app loads when offline');
});
```
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>All test files exist. npm test runs and reports todo tests (not failures). Test infrastructure is complete.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm test -- --run` executes without errors
2. Test output shows todo tests in each file (not failures)
3. All test files exist: tests/pwa.test.ts, tests/db/crud.test.ts, tests/persistence.test.ts, tests/offline.test.ts
4. vitest.config.ts configures jsdom environment and setup file
</verification>

<success_criteria>
- Vitest configured with jsdom environment
- fake-indexeddb installed and configured in setup file
- All test file scaffolds created with todo markers
- npm test executes without configuration errors
- Test files ready to be populated during implementation plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data/01-00-SUMMARY.md`
</output>
