---
phase: 03-context-lookup
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tests/hooks/useSearch.test.ts
  - tests/hooks/useContexts.test.ts
  - tests/views/SearchView.test.tsx
  - tests/views/ContextBrowseView.test.tsx
  - tests/components/GroupedResults.test.tsx
  - tests/utils/groupByContext.test.ts
autonomous: true
requirements: [LOOK-01, LOOK-02, LOOK-03, LOOK-04]

must_haves:
  truths:
    - "Test scaffolds exist for all Phase 3 features"
    - "Fuse.js dependency is installed and available for import"
    - "All test files are discovered by Vitest without errors"
  artifacts:
    - path: "package.json"
      provides: "fuse.js dependency"
      contains: "fuse.js"
    - path: "tests/hooks/useSearch.test.ts"
      provides: "Test scaffold for search hook"
      min_lines: 20
    - path: "tests/hooks/useContexts.test.ts"
      provides: "Test scaffold for contexts hook"
      min_lines: 20
    - path: "tests/views/SearchView.test.tsx"
      provides: "Test scaffold for search view"
      min_lines: 20
    - path: "tests/views/ContextBrowseView.test.tsx"
      provides: "Test scaffold for browse view"
      min_lines: 20
    - path: "tests/components/GroupedResults.test.tsx"
      provides: "Test scaffold for grouped results"
      min_lines: 20
    - path: "tests/utils/groupByContext.test.ts"
      provides: "Test scaffold for grouping utility"
      min_lines: 15
  key_links:
    - from: "tests/hooks/useSearch.test.ts"
      to: "fuse.js"
      via: "import statement"
      pattern: "import.*fuse"
    - from: "all test files"
      to: "vitest"
      via: "test discovery"
      pattern: "describe|test|it"
---

<objective>
Establish test infrastructure for Phase 3 context lookup features by installing Fuse.js and creating test scaffolds with .todo markers for all components, hooks, views, and utilities.

Purpose: Following Phase 2's successful TDD pattern, Wave 0 scaffolds prevent test discovery failures and enable smooth RED-GREEN cycles in later waves. Nyquist compliance requires test scaffolds before implementation.

Output: 6 test files with .todo markers, fuse.js installed, npm test runs without errors
</objective>

<execution_context>
@C:/Users/jshepherd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshepherd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/PROJECT.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/ROADMAP.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/STATE.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/phases/03-context-lookup/03-RESEARCH.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/phases/03-context-lookup/03-VALIDATION.md
</context>

<interfaces>
<!-- Existing types from codebase -->

From src/types/Person.ts:
```typescript
export interface Person {
  id?: number;
  name: string;
  context: string;
  notes?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

From src/db/schema.ts:
```typescript
export class NameHelprDB extends Dexie {
  persons!: Table<Person>;
}
export const db: NameHelprDB;
```

From src/db/hooks.ts:
```typescript
export function usePersons(): Person[] | undefined;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Fuse.js dependency</name>
  <files>package.json, package-lock.json</files>
  <action>
Install fuse.js (client-side fuzzy search library) as production dependency:

```bash
npm install fuse.js
```

Verify installation:
- package.json contains "fuse.js" in dependencies
- package-lock.json updated with fuse.js and its dependencies
- Can import Fuse in test: `import Fuse from 'fuse.js';` compiles without errors

Per 03-RESEARCH.md: Fuse.js is 3KB gzipped, industry standard (438K weekly downloads), provides fuzzy matching for typo tolerance.
  </action>
  <verify>
    <automated>npm list fuse.js</automated>
  </verify>
  <done>fuse.js appears in package.json dependencies, npm list shows installed version</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffolds for hooks</name>
  <files>tests/hooks/useSearch.test.ts, tests/hooks/useContexts.test.ts</files>
  <action>
Create two test scaffold files following Phase 2 pattern (see 02-00-SUMMARY.md):

**tests/hooks/useSearch.test.ts** (covers LOOK-01):
```typescript
import { describe, it } from 'vitest';

describe('useSearch', () => {
  it.todo('returns all people when query is empty');
  it.todo('filters people by fuzzy matching on name and context');
  it.todo('handles typos with threshold 0.4');
  it.todo('prioritizes context matches over name matches');
  it.todo('returns results in under 100ms for 200 people');
});
```

**tests/hooks/useContexts.test.ts** (covers LOOK-02):
```typescript
import { describe, it } from 'vitest';

describe('useContexts', () => {
  it.todo('returns distinct contexts from all people');
  it.todo('includes count of people per context');
  it.todo('sorts contexts alphabetically');
  it.todo('updates reactively when person added with new context');
});
```

Use .todo() markers so Vitest discovers tests without failing. Tests will be implemented in Wave 1 and Wave 2.
  </action>
  <verify>
    <automated>npm test -- --run tests/hooks/</automated>
  </verify>
  <done>Both test files exist, Vitest discovers tests without errors, shows as "todo" in output</done>
</task>

<task type="auto">
  <name>Task 3: Create test scaffolds for views and components</name>
  <files>tests/views/SearchView.test.tsx, tests/views/ContextBrowseView.test.tsx, tests/components/GroupedResults.test.tsx, tests/utils/groupByContext.test.ts</files>
  <action>
Create four test scaffold files:

**tests/views/SearchView.test.tsx** (covers LOOK-01 integration):
```typescript
import { describe, it } from 'vitest';

describe('SearchView', () => {
  it.todo('renders search input at top of screen');
  it.todo('shows hint text when query is empty');
  it.todo('displays results as user types');
  it.todo('shows "no results" when query has no matches');
});
```

**tests/views/ContextBrowseView.test.tsx** (covers LOOK-02, LOOK-03):
```typescript
import { describe, it } from 'vitest';

describe('ContextBrowseView', () => {
  it.todo('renders list of contexts alphabetically');
  it.todo('shows person count for each context');
  it.todo('navigates to context detail on tap');
  it.todo('shows empty state when no people exist');
});
```

**tests/components/GroupedResults.test.tsx** (covers LOOK-04):
```typescript
import { describe, it } from 'vitest';

describe('GroupedResults', () => {
  it.todo('groups people by context');
  it.todo('sorts contexts alphabetically');
  it.todo('shows person name and notes in each group');
  it.todo('calls onPersonClick when person tapped');
});
```

**tests/utils/groupByContext.test.ts** (covers LOOK-04 unit):
```typescript
import { describe, it } from 'vitest';

describe('groupByContext', () => {
  it.todo('groups people array by context field');
  it.todo('handles empty array');
  it.todo('handles single context with multiple people');
});
```

Create tests/views/ directory if it doesn't exist. Use .todo() markers for all tests.
  </action>
  <verify>
    <automated>npm test -- --run tests/views/ tests/components/GroupedResults tests/utils/groupByContext</automated>
  </verify>
  <done>All 4 test files exist, Vitest discovers all tests as "todo", no errors during discovery</done>
</task>

</tasks>

<verification>
Overall phase infrastructure checks:
- All 6 test files created with .todo markers
- fuse.js installed in package.json
- npm test runs entire suite without errors
- Test count increases by expected number of todos
</verification>

<success_criteria>
- fuse.js appears in package.json dependencies
- 6 new test files exist: useSearch, useContexts, SearchView, ContextBrowseView, GroupedResults, groupByContext
- All tests marked .todo (discoverable but not failing)
- npm test -- --run completes successfully
- Wave 1, 2, 3 plans can reference these test files for RED-GREEN cycles
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-lookup/03-00-SUMMARY.md`
</output>
