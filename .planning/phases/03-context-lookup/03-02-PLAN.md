---
phase: 03-context-lookup
plan: 02
type: tdd
wave: 2
depends_on: [03-00]
files_modified:
  - src/hooks/useContexts.ts
  - tests/hooks/useContexts.test.ts
  - src/views/ContextBrowseView.tsx
  - tests/views/ContextBrowseView.test.tsx
autonomous: true
requirements: [LOOK-02, LOOK-03]

must_haves:
  truths:
    - "User can see list of all contexts sorted alphabetically"
    - "Each context shows count of people associated with it"
    - "User can tap a context to see filtered people from that context"
    - "Context list updates when new person added with new context"
  artifacts:
    - path: "src/hooks/useContexts.ts"
      provides: "Hook for distinct contexts with counts"
      min_lines: 25
      exports: ["useContexts", "ContextWithCount"]
    - path: "src/views/ContextBrowseView.tsx"
      provides: "Browse all contexts UI"
      min_lines: 50
      exports: ["ContextBrowseView"]
  key_links:
    - from: "src/hooks/useContexts.ts"
      to: "dexie-react-hooks"
      via: "useLiveQuery for reactive updates"
      pattern: "useLiveQuery\\("
    - from: "src/hooks/useContexts.ts"
      to: "src/db/schema.ts"
      via: "db.persons.toArray()"
      pattern: "db\\.persons"
    - from: "src/views/ContextBrowseView.tsx"
      to: "src/hooks/useContexts.ts"
      via: "hook call"
      pattern: "useContexts\\(\\)"
---

<objective>
Implement context browsing functionality that shows all distinct contexts with person counts, enabling users to browse and filter by context.

Purpose: Users often want to browse all contexts to jog memory ("Was it Paul's party or Jim's party?"). Alphabetical sorting makes scanning easy on mobile.

Output: useContexts hook with useLiveQuery integration, ContextBrowseView component with tappable context list
</objective>

<execution_context>
@C:/Users/jshepherd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshepherd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/PROJECT.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/ROADMAP.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/phases/03-context-lookup/03-RESEARCH.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/phases/01-foundation-data/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Types and exports needed for this plan -->

From src/types/Person.ts:
```typescript
export interface Person {
  id?: number;
  name: string;
  context: string;
  notes?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

From src/db/schema.ts:
```typescript
export class NameHelprDB extends Dexie {
  persons!: Table<Person>;
}
export const db: NameHelprDB;
```

From dexie-react-hooks (already installed):
```typescript
export function useLiveQuery<T>(querier: () => Promise<T>, deps?: any[]): T | undefined;
```

From src/components/PersonList.tsx (existing, can reuse for context detail):
```typescript
export interface PersonListProps {
  people: Person[];
  onEdit?: (person: Person) => void;
  onDelete?: (id: number) => void;
}
export function PersonList(props: PersonListProps): JSX.Element;
```
</interfaces>

<feature>
  <name>Context Browsing with Filtering</name>
  <files>src/hooks/useContexts.ts, tests/hooks/useContexts.test.ts, src/views/ContextBrowseView.tsx, tests/views/ContextBrowseView.test.tsx</files>
  <behavior>
**useContexts hook behavior:**
- Returns: ContextWithCount[] | undefined
- ContextWithCount: { context: string; count: number }
- Loads all people via db.persons.toArray()
- Counts people per context using reduce
- Returns distinct contexts sorted alphabetically
- Uses useLiveQuery for reactive updates (auto-refreshes when people added/deleted)
- Returns undefined while loading (standard Dexie pattern)

**ContextBrowseView component behavior:**
- Shows loading state while contexts is undefined
- Renders empty state when contexts.length === 0 ("No people yet")
- Renders list of contexts sorted alphabetically
- Each context item shows: context name + person count (e.g., "Paul's Party (3)")
- Context items are tappable (44px min height per mobile guidelines)
- Tapping context shows filtered list of people from that context
- Uses PersonList component (existing) to display filtered people
- Back button returns to context list

**Test cases:**
- useContexts: returns distinct contexts from all people
- useContexts: includes count of people per context
- useContexts: sorts contexts alphabetically
- useContexts: updates reactively when person added (test with fake-indexeddb)
- ContextBrowseView: renders list of contexts alphabetically
- ContextBrowseView: shows person count for each context
- ContextBrowseView: shows filtered people on context tap
- ContextBrowseView: shows empty state when no people exist
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Write failing tests for context browsing</name>
  <files>tests/hooks/useContexts.test.ts, tests/views/ContextBrowseView.test.tsx</files>
  <behavior>
    - useContexts returns distinct contexts from all people
    - useContexts includes count of people per context
    - useContexts sorts contexts alphabetically
    - useContexts updates reactively when person added
    - ContextBrowseView renders contexts alphabetically
    - ContextBrowseView shows person count for each context
    - ContextBrowseView shows filtered people on context tap
    - ContextBrowseView shows empty state when no people exist
  </behavior>
  <action>
Replace .todo markers in tests/hooks/useContexts.test.ts with real tests:
- Test 1: Returns distinct contexts (add 3 people, 2 contexts → returns 2)
- Test 2: Includes counts ({ context: "A", count: 2 }, { context: "B", count: 1 })
- Test 3: Sorts alphabetically (add contexts in order C, A, B → returns [A, B, C])
- Test 4: Updates reactively (add person, verify contexts updates)

Replace .todo markers in tests/views/ContextBrowseView.test.tsx:
- Test 1: Renders contexts alphabetically
- Test 2: Shows count for each context
- Test 3: Shows filtered people on tap (click context, verify PersonList rendered)
- Test 4: Shows empty state when no people

Use fake-indexeddb for database tests (already configured in Phase 1). Use @testing-library/react for component tests.

Run tests - MUST fail with expected errors.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>Tests exist and fail with expected errors. Commit: test(03-02): add failing tests for context browsing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement context browsing to pass tests</name>
  <files>src/hooks/useContexts.ts, src/views/ContextBrowseView.tsx</files>
  <action>
Implement src/hooks/useContexts.ts following 03-RESEARCH.md Pattern 3:
- Export ContextWithCount interface
- Use useLiveQuery(() => { ... }, [])
- Load people via db.persons.toArray()
- Reduce to count per context: Record<string, number>
- Convert to array with Object.entries().map()
- Sort with localeCompare()

Implement src/views/ContextBrowseView.tsx:
- Import useContexts, PersonList, useState for selected context
- Render loading state if !contexts
- Render empty state if contexts.length === 0
- Render context list if contexts exists
- Each context: button/div with onClick={() => setSelectedContext(context)}
- If selectedContext: render PersonList with filtered people (db.persons.where({ context: selectedContext }).toArray())
- Back button: setSelectedContext(null)
- Mobile CSS: 44px min-height, clear touch feedback, sticky headers

Run tests - MUST pass all 8 tests.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>All tests pass. Commit: feat(03-02): implement context browsing</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR - Clean up if needed</name>
  <files>src/hooks/useContexts.ts, src/views/ContextBrowseView.tsx</files>
  <action>
Optional refactoring (only if code is complex):
- Extract ContextListItem if styling becomes complex
- Consider using React Router for context detail navigation (defer to Phase 4 if needed)
- Ensure clean separation of concerns

Run tests after refactoring - MUST still pass.

If no refactoring needed, skip this task.
  </action>
  <verify>
    <automated>npm test -- --run && npm run build</automated>
  </verify>
  <done>Code is clean, tests pass, TypeScript compiles. Commit if changes made: refactor(03-02): clean up context browsing</done>
</task>

</tasks>

<verification>
RED-GREEN-REFACTOR cycle complete:
- RED commit: Tests fail with expected errors
- GREEN commit: All tests pass
- Build succeeds: npm run build
- Full test suite green: npm test -- --run
- Context list updates reactively (verified by test)
</verification>

<success_criteria>
- useContexts hook returns ContextWithCount[] sorted alphabetically
- ContextBrowseView renders context list with counts
- Tapping context shows filtered people from that context
- Adding new person with new context updates list immediately
- All 8 tests pass (4 hook + 4 view)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-lookup/03-02-SUMMARY.md`
</output>
