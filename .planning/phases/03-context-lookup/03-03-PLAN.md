---
phase: 03-context-lookup
plan: 03
type: tdd
wave: 3
depends_on: [03-00, 03-01]
files_modified:
  - src/utils/groupByContext.ts
  - tests/utils/groupByContext.test.ts
  - src/components/GroupedResults.tsx
  - tests/components/GroupedResults.test.tsx
  - src/views/SearchView.tsx
autonomous: true
requirements: [LOOK-04]

must_haves:
  truths:
    - "Search results are grouped by context"
    - "Each context group shows context name as header"
    - "People within each group show name and notes"
    - "Context groups are sorted alphabetically"
  artifacts:
    - path: "src/utils/groupByContext.ts"
      provides: "Utility to group people by context"
      min_lines: 15
      exports: ["groupByContext", "GroupedResults"]
    - path: "src/components/GroupedResults.tsx"
      provides: "Grouped results display component"
      min_lines: 60
      exports: ["GroupedResults"]
  key_links:
    - from: "src/components/GroupedResults.tsx"
      to: "src/utils/groupByContext.ts"
      via: "grouping function call"
      pattern: "groupByContext\\(results"
    - from: "src/views/SearchView.tsx"
      to: "src/components/GroupedResults.tsx"
      via: "component render"
      pattern: "<GroupedResults"
---

<objective>
Implement grouped results display for multi-context search, organizing search results by context with visual hierarchy for easier mobile scanning.

Purpose: When searching across multiple contexts (e.g., "Paul" returns matches from "Paul's Party", "Jim's Friends", "Concert 2024"), grouping by context provides visual hierarchy and reduces cognitive load on mobile.

Output: groupByContext utility function, GroupedResults component, SearchView updated to use grouped display
</objective>

<execution_context>
@C:/Users/jshepherd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshepherd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/PROJECT.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/ROADMAP.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/phases/03-context-lookup/03-RESEARCH.md
@C:/Users/jshepherd/source/GSD/NameHelpr/.planning/phases/03-context-lookup/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Types and exports needed for this plan -->

From src/types/Person.ts:
```typescript
export interface Person {
  id?: number;
  name: string;
  context: string;
  notes?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

From src/hooks/useSearch.ts (created in 03-01):
```typescript
export function useSearch(people: Person[]): {
  query: string;
  setQuery: (query: string) => void;
  results: Person[];
}
```

From src/views/SearchView.tsx (created in 03-01, will be modified):
Current implementation shows results as simple list. This plan adds grouped display.
</interfaces>

<feature>
  <name>Grouped Search Results by Context</name>
  <files>src/utils/groupByContext.ts, tests/utils/groupByContext.test.ts, src/components/GroupedResults.tsx, tests/components/GroupedResults.test.tsx, src/views/SearchView.tsx</files>
  <behavior>
**groupByContext function behavior:**
- Input: Person[]
- Output: GroupedResults = { [context: string]: Person[] }
- Groups people by context field using reduce
- Returns object with context as key, array of people as value
- Handles empty array (returns empty object)
- Handles single context (returns object with one key)

**GroupedResults component behavior:**
- Props: { results: Person[], onPersonClick?: (person: Person) => void }
- Calls groupByContext(results) to organize data
- Extracts context keys and sorts alphabetically
- Renders sections for each context:
  - Context header (sticky, background color, uppercase text)
  - List of people in that context (name + notes if present)
- Each person item is tappable (calls onPersonClick)
- 44px min-height for touch targets
- Shows "No results" if results.length === 0

**SearchView integration:**
- Replace simple results list with GroupedResults component
- Pass results from useSearch to GroupedResults
- Pass onPersonClick handler (if needed for navigation to detail)
- Keep search input and empty state handling unchanged

**Test cases:**
- groupByContext: groups people by context field
- groupByContext: handles empty array
- groupByContext: handles single context with multiple people
- GroupedResults: groups people by context
- GroupedResults: sorts contexts alphabetically
- GroupedResults: shows person name and notes
- GroupedResults: calls onPersonClick when person tapped
- SearchView: renders GroupedResults component with search results
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Write failing tests for grouped results</name>
  <files>tests/utils/groupByContext.test.ts, tests/components/GroupedResults.test.tsx, tests/views/SearchView.test.tsx</files>
  <behavior>
    - groupByContext groups people by context field
    - groupByContext returns empty object for empty array
    - groupByContext handles single context with multiple people
    - GroupedResults groups people by context (sections rendered)
    - GroupedResults sorts contexts alphabetically
    - GroupedResults shows person name and notes
    - GroupedResults calls onPersonClick when person tapped
    - SearchView renders GroupedResults with search results
  </behavior>
  <action>
Replace .todo markers in tests/utils/groupByContext.test.ts:
- Test 1: Groups people array by context field
- Test 2: Returns empty object for empty array
- Test 3: Handles single context with multiple people

Replace .todo markers in tests/components/GroupedResults.test.tsx:
- Test 1: Groups people by context (verify sections rendered)
- Test 2: Sorts contexts alphabetically (C, A, B input → renders A, B, C order)
- Test 3: Shows person name and notes in each group
- Test 4: Calls onPersonClick when person tapped

Add test to tests/views/SearchView.test.tsx:
- Test: Renders GroupedResults with search results

Run tests - MUST fail with expected errors.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>Tests exist and fail with expected errors. Commit: test(03-03): add failing tests for grouped results</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement grouped results to pass tests</name>
  <files>src/utils/groupByContext.ts, src/components/GroupedResults.tsx, src/views/SearchView.tsx</files>
  <action>
Implement src/utils/groupByContext.ts following 03-RESEARCH.md Pattern 2:
```typescript
import { Person } from '../types/Person';

export interface GroupedResults {
  [context: string]: Person[];
}

export function groupByContext(people: Person[]): GroupedResults {
  return people.reduce((groups, person) => {
    const context = person.context;
    if (!groups[context]) {
      groups[context] = [];
    }
    groups[context].push(person);
    return groups;
  }, {} as GroupedResults);
}
```

Implement src/components/GroupedResults.tsx:
- Import groupByContext, Person type
- Component props: { results: Person[], onPersonClick?: (person: Person) => void }
- Call grouped = groupByContext(results)
- Extract contexts = Object.keys(grouped).sort()
- Render sections for each context (map over contexts)
- Each section: header + list of people
- CSS: sticky headers, 44px touch targets, active state feedback

Update src/views/SearchView.tsx:
- Import GroupedResults component
- Replace simple results list with <GroupedResults results={results} />
- Keep search input and empty/no-results handling unchanged

CSS for GroupedResults (add to App.css or separate file):
- .context-group: margin-bottom 1.5rem
- .context-header: sticky top 0, background #f5f5f5, uppercase, padding
- .person-list-item: 44px min-height, padding, border-bottom, active state

Run tests - MUST pass all 8 tests.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>All tests pass. Commit: feat(03-03): implement grouped search results</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR - Clean up if needed</name>
  <files>src/components/GroupedResults.tsx, src/views/SearchView.tsx</files>
  <action>
Optional refactoring (only if code is complex):
- Extract PersonListItem if reused across components
- Consider extracting CSS to separate module
- Ensure clean separation of concerns

Run tests after refactoring - MUST still pass.

If no refactoring needed, skip this task.
  </action>
  <verify>
    <automated>npm test -- --run && npm run build</automated>
  </verify>
  <done>Code is clean, tests pass, TypeScript compiles. Commit if changes made: refactor(03-03): clean up grouped results</done>
</task>

</tasks>

<verification>
RED-GREEN-REFACTOR cycle complete:
- RED commit: Tests fail with expected errors
- GREEN commit: All tests pass
- Build succeeds: npm run build
- Full test suite green: npm test -- --run
- Grouped display works visually (manual check in browser)
</verification>

<success_criteria>
- groupByContext utility exists and groups people by context
- GroupedResults component renders sections with context headers
- SearchView uses GroupedResults for multi-context results
- Contexts are sorted alphabetically within GroupedResults
- All 8 tests pass (3 utility + 4 component + 1 integration)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-lookup/03-03-SUMMARY.md`
</output>
