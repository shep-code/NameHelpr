---
phase: 02-quick-entry
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/hooks/useDebounce.test.ts
  - tests/components/AutoSaveForm.test.tsx
  - tests/components/ContextAutocomplete.test.tsx
autonomous: true
requirements:
  - ENTR-01
  - ENTR-02
  - ENTR-03
  - ENTR-04

must_haves:
  truths:
    - "Test scaffolds exist for all Phase 2 requirements before implementation"
    - "Tests can run and fail (RED state) indicating missing implementation"
  artifacts:
    - path: "tests/hooks/useDebounce.test.ts"
      provides: "Debounce hook unit tests"
      contains: "describe.*useDebounce"
    - path: "tests/components/AutoSaveForm.test.tsx"
      provides: "Auto-save form integration tests"
      contains: "describe.*AutoSaveForm"
    - path: "tests/components/ContextAutocomplete.test.tsx"
      provides: "Context autocomplete tests"
      contains: "describe.*ContextAutocomplete"
  key_links:
    - from: "tests/hooks/useDebounce.test.ts"
      to: "src/hooks/useDebounce.ts"
      via: "import"
      pattern: "import.*useDebounce.*from"
---

<objective>
Create test scaffolds for all Phase 2 features before implementation begins.

Purpose: Establish RED state tests that will turn GREEN as features are implemented (Wave 0 requirement per Nyquist rule)
Output: Test files with failing tests for useDebounce hook, AutoSaveForm component, and ContextAutocomplete component
</objective>

<execution_context>
@C:/Users/jshepherd/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshepherd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quick-entry/02-RESEARCH.md

<interfaces>
<!-- Existing interfaces from Phase 1 that tests will use -->

From src/types/Person.ts:
```typescript
export interface Person {
  id?: number;
  name: string;
  context: string;
  notes?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

From src/db/hooks.ts:
```typescript
export function usePersons(): {
  persons: Person[] | undefined;
  addPerson: (name: string, context: string, notes?: string) => Promise<number>;
  updatePerson: (id: number, updates: Partial<Person>) => Promise<void>;
  deletePerson: (id: number) => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useDebounce hook test scaffold</name>
  <files>tests/hooks/useDebounce.test.ts</files>
  <action>
Create test file for useDebounce hook with failing tests covering:
1. Returns initial value immediately
2. Delays value update by specified ms
3. Cancels pending update on unmount (no memory leak)
4. Resets delay timer on new value before timeout

Use Vitest fake timers (vi.useFakeTimers, vi.advanceTimersByTime) per RESEARCH.md Pattern 1.
Import from '../src/hooks/useDebounce' (file doesn't exist yet - will fail).

Test structure:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
// import { useDebounce } from '../../src/hooks/useDebounce';

describe('useDebounce', () => {
  beforeEach(() => { vi.useFakeTimers(); });
  afterEach(() => { vi.useRealTimers(); });

  it.todo('returns initial value immediately');
  it.todo('delays value update by specified delay');
  it.todo('cancels pending update on unmount');
  it.todo('resets timer when value changes before delay');
});
```

Note: Keep tests as `.todo` stubs initially since the hook doesn't exist yet. The scaffold itself passing is the verification.
  </action>
  <verify>
    <automated>npm test -- tests/hooks/useDebounce.test.ts --run</automated>
  </verify>
  <done>Test file exists with 4 todo test cases for useDebounce behavior</done>
</task>

<task type="auto">
  <name>Task 2: Create AutoSaveForm and ContextAutocomplete test scaffolds</name>
  <files>tests/components/AutoSaveForm.test.tsx, tests/components/ContextAutocomplete.test.tsx</files>
  <action>
Create two component test files with failing tests.

**tests/components/AutoSaveForm.test.tsx** - covers ENTR-01, ENTR-02, ENTR-03:
```typescript
import { describe, it } from 'vitest';
// import { render, screen } from '@testing-library/react';
// import { AutoSaveForm } from '../../src/components/AutoSaveForm';

describe('AutoSaveForm', () => {
  describe('ENTR-01: Single-screen layout', () => {
    it.todo('renders name, context, and notes fields on single view');
    it.todo('all fields visible without scrolling (uses svh units)');
  });

  describe('ENTR-02: Mobile keyboard attributes', () => {
    it.todo('name input has autocapitalize="words"');
    it.todo('context input has autocapitalize="none"');
    it.todo('all inputs have inputMode="text"');
    it.todo('inputs have min-height 44px for touch targets');
  });

  describe('ENTR-03: Auto-save behavior', () => {
    it.todo('saves after 300ms debounce when user stops typing');
    it.todo('does not save on every keystroke');
    it.todo('batches multiple field changes into single save');
    it.todo('shows saving indicator during debounce');
  });
});
```

**tests/components/ContextAutocomplete.test.tsx** - covers ENTR-04:
```typescript
import { describe, it } from 'vitest';
// import { render, screen } from '@testing-library/react';
// import { ContextAutocomplete } from '../../src/components/ContextAutocomplete';

describe('ContextAutocomplete', () => {
  describe('ENTR-04: Context suggestions', () => {
    it.todo('renders input with datalist element');
    it.todo('datalist contains distinct contexts from existing persons');
    it.todo('updates suggestions when new person added');
    it.todo('suggestions are case-preserving');
  });
});
```

Both files use `.todo` tests as scaffolds. The scaffold files existing is the success criteria.
  </action>
  <verify>
    <automated>npm test -- tests/components --run</automated>
  </verify>
  <done>Both test scaffold files exist with todo markers for all Phase 2 requirements</done>
</task>

</tasks>

<verification>
After Wave 0 completion:
1. `npm test -- --run` shows all new test files discovered
2. Todo tests are listed but don't fail the suite (Vitest skips `.todo`)
3. Test file structure matches VALIDATION.md requirements map
</verification>

<success_criteria>
- tests/hooks/useDebounce.test.ts exists with 4 todo tests
- tests/components/AutoSaveForm.test.tsx exists with ~10 todo tests covering ENTR-01, ENTR-02, ENTR-03
- tests/components/ContextAutocomplete.test.tsx exists with 4 todo tests covering ENTR-04
- `npm test -- --run` passes (todo tests are skipped, not failed)
- Directory structure: tests/hooks/ and tests/components/ created
</success_criteria>

<output>
After completion, create `.planning/phases/02-quick-entry/02-00-SUMMARY.md`
</output>
